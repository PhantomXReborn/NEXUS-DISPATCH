# Create project directory
mkdir nexus-dispatch
cd nexus-dispatch

# Initialize npm project
npm init -y

# Create folder structure
mkdir -p public/css public/js server/routes server/models migrations seeds